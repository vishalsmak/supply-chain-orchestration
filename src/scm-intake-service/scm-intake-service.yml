apiVersion: apps/v1
kind: Deployment
metadata:
  name: scm-intake-service
  labels:
    app: scm-intake-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scm-intake-service
  template:
    metadata:
      labels:
        app: scm-intake-service
    spec:
      containers:
        - name: scm-intake-service
          image: scm-intake-service
          imagePullPolicy: Never